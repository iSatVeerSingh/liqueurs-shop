<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8"/>
		<title>Badwater Liquers Shop</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"/>
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
		<link rel="stylesheet" href="/style.css"/>
		<style>
			/* Custom CSS */
			.product-image {
				max-width: 100%;
				height: auto;
			}
			.breadcrumb {
				background-color: transparent;
				padding: 0;
				margin-bottom: 1rem;
			}
			.product-meta li {
				margin-bottom: 0.25rem;
			}
			.rewards-box {
				border: 1px solid #dee2e6;
				padding: 1rem;
				margin-top: 1rem;
			}
		</style>
	</head>
	<body>
		{% include 'navbar.twig' with { 'navbarData': navbarData } %}
		<div
			class="container my-4">
			<!-- Breadcrumb -->
			<nav aria-label="breadcrumb">
				<ol class="breadcrumb">
					<li class="breadcrumb-item">
						<a href="/">Home</a>
					</li>
					<li class="breadcrumb-item active" aria-current="page">
						{{ product.bottle }}
					</li>
				</ol>
			</nav>

			<div
				class="row">
				<!-- Left Column: Product Image -->
				<div class="col-md-4 text-center">
					<img src="{{ product.image }}" alt="{{ product.bottle }}" class="product-image img-fluid"/>
				</div>

				<!-- Right Column: Product Details -->
				<div
					class="col-md-8">
					<!-- Product Title & Price -->
					<h1
						class="h4 mb-2">
						<!-- Example name -->
						{{ product.bottle }}
					</h1>
					<p
						class="fs-5 text-danger mb-4">
						<!-- Example price -->
						{{ product.value }}
					</p>

					<!-- Size, Proof, and other meta data -->
					<ul class="list-unstyled product-meta mb-4">
						<li>
							<strong>1oz:</strong>
							{{ product.price_1_oz }}
						</li>
						<li>
							<strong>Age:</strong>
							{{ product.age != "NAS" ? age ~ " Years" : "NAS" }}</li>
						<li>
							<strong>Proof:</strong>
							{{ product.proof }}</li>
					</ul>

					<!-- Description -->
					<p class="fs-5 fw-bold">
						About
						{{ product.bottle }}</p>
					<p>
						{{ product.description }}
					</p>
				</div>
			</div>
		</div>

		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
		<script src="/navbar.js"></script>
	</body>
</html>
